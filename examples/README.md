# Examples

Small, shareable example inputs can live here later.

Do not commit real FASTQ files, `.md5` files, or lab datasets to this
repository. SIMPLseq-nf App reads large FASTQ datasets from a folder path on disk;
it does not upload sequencing data through the browser.
